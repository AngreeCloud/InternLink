import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Aprovações de Alunos",
};

export default function AprovacoesProfessorLayout({ children }: { children: React.ReactNode }) {
  return children;
}
