import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Estágios",
};

export default function EstagiosProfessorLayout({ children }: { children: React.ReactNode }) {
  return children;
}
