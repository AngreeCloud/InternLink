import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Documentos",
};

export default function DocumentosProfessorLayout({ children }: { children: React.ReactNode }) {
  return children;
}
