import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Documentos do Tutor",
};

export default function DocumentosTutorLayout({ children }: { children: React.ReactNode }) {
  return children;
}
