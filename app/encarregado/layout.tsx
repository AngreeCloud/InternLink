import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Encarregado de Educação – InternLink",
};

export default function EncarregadoRootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
